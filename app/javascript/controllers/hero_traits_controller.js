import { Controller } from "@hotwired/stimulus"

// This controller is reserved for future inline trait creation functionality.
// The basic hero form now works without any JavaScript - it's a standard Rails form.
export default class extends Controller {
  // Future: Add inline trait creation methods here if needed
}