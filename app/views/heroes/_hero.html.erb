<div id="<%= dom_id hero %>" class="space-y-3">
  <div>
    <h3 class="font-semibold text-lg text-gray-900"><%= hero.name %></h3>
    <p class="text-sm text-gray-600 mt-1"><%= hero.pronouns %> • <%= hero.role.humanize %></p>
  </div>
  <div>
    <p class="text-sm font-medium text-gray-700 mb-1">Traits:</p>
    <div class="flex flex-wrap gap-1">
      <% hero.traits.each do |trait| %>
        <span class="inline-block px-2 py-1 bg-gray-100 text-gray-700 text-xs rounded-md"><%= trait.name %></span>
      <% end %>
    </div>
  </div>
</div>
