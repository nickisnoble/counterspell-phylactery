The doortal awaits! Your runes:

<%= @code %>

They must be cast within 5 minutes, or it will return to the rift!

—

If you need a new set of runes, log in again:
<%= new_session_url %>
(If you did not request this, you can safely ignore this email.)