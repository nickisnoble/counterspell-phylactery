<%= turbo_frame_tag "signin" do %>
  <p class="text-balance mb-2">Runes were conjured and sent to <%= session[:awaiting_login] %> — please cast them hence: </p>
  <%= form_with url: validate_session_path, data: { turbo_frame: "_top" } do |f| %>
    <%= f.text_field :code, required: true, placeholder: "000000", class: "text-center" %>
  <% end %>
<% end %>